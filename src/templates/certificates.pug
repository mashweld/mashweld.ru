extends ./_layout.pug

block append globals
  - var url = 'certificates.html'
  - var canonical = 'certificates/'
block menu
  +menu({active: 'certificates'})

block content
  .section
    .section__head
      h2.section__title Сертификаты
    .section__body
      +b('ul').thumb-list
        +e('li').item
          +e('a').thumb#certificate_parker.glightbox(data-gallery='certificates', href='img/content/certificates/parker.jpg', data-title='Официальный дилер компании Parker Torchology.')
            +e('img').image(src='img/content/certificates/parker_thumb.jpg', width='240', height='162', alt='Официальный дилер компании Parker Torchology.', loading='lazy')
        +e('li').item
          +e('a').thumb#certificate_mtg.glightbox(data-gallery='certificates', href='img/content/certificates/mtg.jpg', data-title='Официальный дилер компании MTG.')
            +e('img').image(src='img/content/certificates/mtg_thumb.jpg', width='240', height='162', alt='Официальный дилер компании MTG.', loading='lazy')
        +e('li').item
          +e('a').thumb#certificate_ptk.glightbox(data-gallery='certificates', href='img/content/certificates/ptk.jpg', data-title='Официальный дилер компании ООО «ПТК».')
            +e('img').image(src='img/content/certificates/ptk_thumb.jpg', width='240', height='162', alt='Официальный дилер компании ООО «ПТК».', loading='lazy')
        +e('li').item
          +e('a').thumb#certificate_ewm.glightbox(data-gallery='certificates', href='img/content/certificates/ewm.jpg', data-title='Официальный дилер продукции EWM')
            +e('img').image(src='img/content/certificates/ewm_thumb.jpg', width='240', height='162', alt='Официальный дилер продукции EWM', loading='lazy')
        +e('li').item
          +e('a').thumb#certificate_ewm_validation.glightbox(data-gallery='certificates', href='img/content/certificates/ewm_validation.jpg', data-title='Услуги по поверке сварочного оборудования с применением оборудования фирмы EWM')
            +e('img').image(src='img/content/certificates/ewm_validation_thumb.jpg', width='240', height='162', alt='Услуги по поверке сварочного оборудования с применением оборудования фирмы EWM', loading='lazy')
        +e('li').item
          +e('a').thumb#certificate_koike.glightbox(data-gallery='certificates', href='img/content/certificates/koike.jpg', data-title='Официальный дилер продукции KOIKE')
            +e('img').image(src='img/content/certificates/koike_thumb.jpg', width='240', height='162', alt='Официальный дилер продукции KOIKE', loading='lazy')
        +e('li').item
          +e('a').thumb#certificate_blueweld.glightbox(data-gallery='certificates', href='img/content/certificates/blueweld.jpg', data-title='Официальный дилер продукции BLUEWELD')
            +e('img').image(src='img/content/certificates/blueweld_thumb.jpg', width='240', height='162', alt='Официальный дилер продукции BLUEWELD', loading='lazy')
        +e('li').item
          +e('a').thumb#certificate_fubag.glightbox(data-gallery='certificates', href='img/content/certificates/fubag.jpg', data-title='Официальный дилер продукции FUBAG')
            +e('img').image(src='img/content/certificates/fubag_thumb.jpg', width='240', height='162', alt='Официальный дилер продукции FUBAG', loading='lazy')

block append footer_styles
  link(rel='stylesheet', href='vendor/glightbox/glightbox.min.css')
block append footer_scripts
  script(src='vendor/glightbox/glightbox.min.js')
  script.
    (function () {
      var lightbox = GLightbox({
        selector: '.glightbox',
        touchNavigation: true,
        closeOnOutsideClick: true
      });

      var initialHash = window.location.hash;

      lightbox.on('slide_changed', function (prev, slide) {
        var el = slide.trigger;
        if (el && el.id) {
          history.replaceState(null, '', '#' + el.id);
        }
      });

      lightbox.on('close', function () {
        if (initialHash) {
          history.replaceState(null, '', initialHash);
        } else {
          history.replaceState(null, '', window.location.pathname + window.location.search);
        }
      });

      if (initialHash) {
        var target = document.querySelector(initialHash);
        if (target && target.classList.contains('glightbox')) {
          target.click();
        }
      }
    })();
